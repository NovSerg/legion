# Legion AI Agent Interface

**Legion** — это современный, мощный веб-интерфейс для взаимодействия с AI-агентами, разработанный для разработчиков и продвинутых пользователей. Построенный на базе **Next.js 16** и **Material UI v6**, он предоставляет гибкую среду для работы с LLM, управления контекстом и интеграции с внешними инструментами через протокол MCP.

## 🚀 Основные Возможности

### 🤖 Мультимодельность

Legion не привязан к одной нейросети. Вы можете мгновенно переключаться между лучшими моделями на рынке через провайдеров **OpenRouter** и **ZAI**:

- **Anthropic**: Claude 3.5 Sonnet, Haiku.
- **OpenAI**: GPT-4o, GPT-4o-mini.
- **Google**: Gemini Pro 1.5.
- **Alibaba**: Qwen 2.5 (включая Coder и Math версии).
- **Zhipu AI**: GLM-4.

### 🧠 RAG (Retrieval-Augmented Generation)

Встроенная система работы с базой знаний позволяет агенту отвечать на вопросы, используя ваши документы.

- **Локальные эмбеддинги**: Используется библиотека `@xenova/transformers` для генерации векторов прямо в браузере (или на сервере), обеспечивая приватность данных.
- **Гибкие режимы**:
  - _Strict_: Ответ только на основе найденных документов.
  - _Hybrid_: Комбинация знаний модели и контекста из документов.
  - _Off_: Обычный чат.
- **Поддержка форматов**: Загрузка Markdown и текстовых файлов.

### 🔌 Model Context Protocol (MCP)

Legion полностью поддерживает стандарт **MCP**, превращая чат-бота в полноценного агента, способного взаимодействовать с внешним миром.

- **Подключение инструментов**: Интеграция с GitHub, файловой системой, базами данных и любыми другими сервисами, поддерживающими MCP.
- **Безопасная архитектура**: MCP-серверы запускаются как локальные подпроцессы на бэкенде Next.js. Связь осуществляется через stdio, что исключает необходимость открывать порты.
- **Управление через UI**:
  - Редактирование конфигурации `mcp-servers.json` прямо в браузере.
  - Просмотр статуса подключения и логов ошибок.
  - Автоматическое обнаружение и отображение доступных инструментов (tools) для каждого сервера.
- **Горячая перезагрузка**: При изменении конфига серверы автоматически перезапускаются для применения новых настроек.

### 👥 Командный Ассистент (Task Manager)

Интегрированная система управления задачами превращает Legion в полноценного помощника для команды разработки.

- **Управление задачами**: Создание, обновление, поиск и фильтрация задач через естественный язык.
- **Умные рекомендации**: Ассистент анализирует приоритеты, дедлайны и зависимости, предлагая, что делать первым.
- **Аналитика команды**: Отслеживание загруженности, статистика по задачам, выявление просроченных и заблокированных задач.
- **RAG + MCP интеграция**: Комбинирует знание проекта (из документации) с актуальными данными о задачах для контекстных рекомендаций.
- **Примеры запросов**:
  - _"Покажи задачи с приоритетом high и предложи, что делать первым"_
  - _"Кто из команды сейчас больше всего загружен?"_
  - _"Создай задачу для рефакторинга RAG системы"_

_Подробнее: [Настройка командного ассистента](docs/team-assistant-setup.md)_

### ⚙️ Гибкая Настройка Агентов

- **Системные промпты**: Создавайте специализированных агентов (например, "Кодер", "Аналитик", "Team Assistant") с уникальными инструкциями.
- **Параметры генерации**: Тонкая настройка Temperature, Top-P, Presence Penalty.
- **Формат ответа**: Принудительный вывод в JSON или XML для автоматизации задач.

## 🛠️ Технологический Стек

Проект построен на современном стеке технологий, обеспечивающем производительность и масштабируемость:

- **Frontend & Backend**: [Next.js 16](https://nextjs.org/) (App Router) — серверный рендеринг и API роуты.
- **UI Framework**: [Material UI v6](https://mui.com/) — адаптивный и красивый интерфейс с поддержкой темной темы.
- **State Management**: [Zustand](https://github.com/pmndrs/zustand) — легкое и быстрое управление состоянием с персистенцией в LocalStorage.
- **AI Integration**:
  - `@modelcontextprotocol/sdk`: Официальный SDK для работы с MCP.
  - `openai`: Клиент для совместимых API (OpenRouter, ZAI).
- **Стилизация**: Emotion (CSS-in-JS).
- **Иконки**: Lucide React и MUI Icons.

## 📦 Установка и Запуск

### Предварительные требования

- Node.js 18+
- npm или yarn

### Шаги установки

1.  **Клонируйте репозиторий**:

    ```bash
    git clone <repository-url>
    cd legion
    ```

2.  **Установите зависимости**:

    ```bash
    npm install
    ```

3.  **Запустите режим разработки**:

    ```bash
    npm run dev
    ```

4.  Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## 🔧 Конфигурация

### 1. API Ключи

Для работы чата необходимо указать ключи доступа к LLM провайдерам.

1.  Нажмите на иконку шестеренки (⚙️) в интерфейсе.
2.  Перейдите на вкладку **API Keys**.
3.  Введите ключи для **OpenRouter** или **ZAI**. Ключи сохраняются только в вашем браузере (LocalStorage).

### 2. Настройка MCP Серверов

Чтобы дать агенту доступ к внешним инструментам (например, к вашим репозиториям GitHub):

1.  Откройте **Settings** -> **MCP Servers**.
2.  В редакторе конфигурации добавьте настройки сервера. Пример для GitHub:
    ```json
    {
      "mcpServers": {
        "github": {
          "command": "npx",
          "args": ["-y", "@modelcontextprotocol/server-github"],
          "env": {
            "GITHUB_PERSONAL_ACCESS_TOKEN": "ваш_токен_здесь"
          }
        }
      }
    }
    ```
    _Примечание: Используйте `npx` для запуска Node.js серверов без предварительной установки._
3.  Нажмите **Save & Sync**. Приложение автоматически скачает нужный пакет и запустит сервер.
4.  После подключения в карточке сервера появятся доступные инструменты (например, `search_repositories`, `read_file`).

## 📚 Руководство Пользователя

### Работа с Чатом

- **Сессии**: Создавайте новые чаты для разных задач. История сохраняется автоматически.
- **Контекст**: Вы можете очистить контекст текущей беседы, не удаляя сам чат, чтобы сэкономить токены.
- **Копирование кода**: Блоки кода в ответах имеют кнопку копирования и подсветку синтаксиса.

### Использование Инструментов (Tools)

Агент сам решает, когда использовать инструменты.

- _Пример_: "Найди в моем репозитории файл package.json".
- Агент увидит, что подключен сервер GitHub, вызовет инструмент `search_code` или `get_file_contents`, получит результат и сформулирует ответ.

## 🤝 Вклад в проект (Contributing)

Мы приветствуем любые улучшения!

1.  Форкните репозиторий.
2.  Создайте ветку для вашей фичи (`git checkout -b feature/amazing-feature`).
3.  Закоммитьте изменения (`git commit -m 'Add amazing feature'`).
4.  Запушьте ветку (`git push origin feature/amazing-feature`).
5.  Откройте Pull Request.

## 📄 Лицензия

Распространяется под лицензией MIT. Подробнее см. файл [LICENSE](LICENSE).
