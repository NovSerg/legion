# Инструкция: Настройка командного ассистента

## Обзор

Командный ассистент объединяет возможности **RAG** (знание проекта), **MCP** (взаимодействие с сервисами) и специализированного агента для управления задачами команды.

**Основные возможности:**

- 📋 Создание и управление задачами через естественный язык
- 📊 Получение статистики и отчётов по задачам
- 🎯 Интеллектуальные рекомендации по приоритетам
- 🔍 Ответы на вопросы о статусе проекта
- 👥 Управление загруженностью команды

---

## Шаги настройки

### 1. Добавьте Task Manager в конфигурацию MCP

Откройте файл конфигурации MCP серверов (`mcp-servers.json` в корне проекта).

Добавьте следующую конфигурацию:

```json
{
  "mcpServers": {
    "git": {
      "command": "node",
      "args": ["mcp-servers/git-server.js"]
    },
    "support-crm": {
      "command": "node",
      "args": ["mcp-servers/support-crm.js"]
    },
    "task-manager": {
      "command": "node",
      "args": ["mcp-servers/task-manager.js"]
    }
  }
}
```

### 2. Перезапустите приложение

```bash
# Остановите текущий dev server (Ctrl+C)
npm run dev
```

### 3. Подключите сервер в интерфейсе

1. Откройте приложение в браузере (http://localhost:3000)
2. В боковой панели найдите раздел **MCP Servers**
3. Сервер `task-manager` должен появиться в списке
4. Нажмите **Connect** для подключения

### 4. Проверьте подключение

После подключения сервер должен показать статус **Connected** и предоставить следующие инструменты:

- `list_tasks` - список задач с фильтрацией
- `get_task` - детали конкретной задачи
- `create_task` - создание новой задачи
- `update_task` - обновление задачи
- `search_tasks` - поиск задач
- `get_stats` - статистика по задачам
- `get_priorities_recommendation` - рекомендации по приоритетам
- `get_team_workload` - загруженность команды

### 5. (Опционально) Загрузите документацию проекта в RAG

Для более умных рекомендаций загрузите документацию проекта:

1. Откройте **Knowledge Manager** (иконка базы данных в sidebar)
2. Нажмите **Upload Files**
3. Выберите файлы документации (например, `README.md`, файлы из `docs/`)
4. Нажмите **Build & Save Index**
5. Дождитесь завершения индексации

### 6. Создайте специализированного агента

1. Откройте **Settings** → **Agents**
2. Создайте нового агента со следующими параметрами:

#### Параметры агента

**Имя:** `Team Assistant`

**System Prompt:**

```
Ты — умный ассистент команды разработки Legion. Твоя задача — помогать с управлением задачами и планированием работы.

ТВОИ ВОЗМОЖНОСТИ:
1. 📋 Управление задачами через MCP инструменты (создание, обновление, поиск)
2. 📊 Аналитика: статистика, загруженность команды, рекомендации по приоритетам
3. 🧠 Знание проекта через RAG систему
4. 🎯 Умные рекомендации на основе приоритетов, дедлайнов и зависимостей

КАК ТЫ РАБОТАЕШЬ:
- Используй инструменты task-manager для получения актуальной информации о задачах
- При вопросах о проекте обращайся к базе знаний (RAG)
- Давай конкретные, практичные рекомендации
- Учитывай контекст: приоритеты, дедлайны, зависимости, загруженность команды
- Предлагай следующие шаги и действия

СТИЛЬ ОБЩЕНИЯ:
- Профессиональный, но дружелюбный
- Структурированные ответы с эмодзи для наглядности
- Конкретные рекомендации, а не общие фразы
- Если нужна дополнительная информация — спрашивай

ПРИМЕРЫ ЗАПРОСОВ:
- "Покажи задачи с приоритетом high и предложи, что делать первым"
- "Создай задачу для рефакторинга RAG системы"
- "Кто из команды сейчас больше всего загружен?"
- "Какие задачи просрочены?"
- "Что нужно сделать для релиза?"

Отвечай на русском языке.
```

**Параметры модели:**

- **Model:** `gpt-4o` (или `gpt-4o-mini` для экономии)
- **Temperature:** `0.3` (для более предсказуемых ответов)
- **RAG Mode:** `Hybrid`
- **RAG Threshold:** `0.3`

3. Сохраните агента

---

## Тестовые сценарии

### Сценарий 1: Получение обзора задач

```
Пользователь: Покажи все задачи с высоким приоритетом

Ожидаемый результат:
- Использует инструмент list_tasks с фильтром priority=high
- Показывает список задач
- Даёт краткий обзор и рекомендации
```

### Сценарий 2: Рекомендации по приоритетам

```
Пользователь: Что мне делать первым?

Ожидаемый результат:
- Использует get_priorities_recommendation
- Анализирует приоритеты, дедлайны, зависимости
- Даёт топ-5 рекомендаций с обоснованием
```

### Сценарий 3: Создание задачи

```
Пользователь: Создай задачу для добавления экспорта в Excel с высоким приоритетом

Ожидаемый результат:
- Использует create_task
- Формирует правильное описание
- Подтверждает создание задачи
```

### Сценарий 4: Статистика команды

```
Пользователь: Покажи загруженность команды

Ожидаемый результат:
- Использует get_team_workload
- Показывает, кто перегружен, у кого есть ресурсы
- Даёт рекомендации по распределению задач
```

### Сценарий 5: Комплексный вопрос (RAG + MCP)

```
Пользователь: Какие задачи связаны с RAG системой и что нужно сделать?

Ожидаемый результат:
- Использует search_tasks для поиска задач по "RAG"
- Обращается к RAG для понимания архитектуры
- Даёт контекстные рекомендации с учётом знаний о проекте
```

### Сценарий 6: Обновление задачи

```
Пользователь: Переведи задачу task-2 в статус done

Ожидаемый результат:
- Использует update_task
- Обновляет статус
- Подтверждает изменение
```

---

## Примеры диалогов

### Пример 1: Планирование дня

**Пользователь:** Доброе утро! Что мне взять в работу сегодня?

**Ассистент:**

```
Доброе утро! 👋

Вот мои рекомендации на сегодня:

🔥 СРОЧНО:
1. task-10: Исправить уязвимость в API авторизации
   - Приоритет: CRITICAL
   - Дедлайн: завтра в 12:00
   - Статус: todo

🎯 ВАЖНО:
2. task-2: Исправить баг с зависанием при загрузке больших файлов
   - Приоритет: HIGH
   - Дедлайн: послезавтра
   - Статус: in-progress (уже начата Марией)

3. task-1: Добавить экспорт в PDF
   - Приоритет: HIGH
   - Дедлайн: 10 декабря

Рекомендую начать с task-10 — это критичная уязвимость безопасности.
```

### Пример 2: Делегирование

**Пользователь:** Кому можно поручить задачу по темной теме?

**Ассистент:**

```
Смотрю загруженность команды... 👥

Текущая загрузка:
- Алексей Петров: 🔴 Перегружен (110%)
- Мария Иванова: 🟡 Высокая (88%)
- Иван Смирнов: 🟡 Высокая (85%)
- Дмитрий Соколов: 🟢 Нормальная (15%)

Рекомендую поручить task-11 (темная тема) **Дмитрию Соколову**:
✅ У него самая низкая загрузка
✅ Это frontend задача (его специализация)
✅ Низкий приоритет — можно делать без спешки

Создать назначение?
```

---

## Troubleshooting

### Сервер не подключается

**Проблема:** Task Manager показывает статус "error" или "disconnected"

**Решение:**

1. Проверьте, что файл `task-manager.js` существует
2. Убедитесь, что Node.js установлен (`node --version`)
3. Проверьте логи в консоли браузера (F12)
4. Попробуйте перезапустить приложение

### Инструменты не отображаются

**Проблема:** После подключения не видно инструментов

**Решение:**

1. Обновите страницу браузера (Ctrl+R)
2. Переподключите сервер (Disconnect → Connect)
3. Проверьте консоль на ошибки

### Ошибка "Failed to load task data"

**Проблема:** Сервер сообщает об ошибке загрузки данных

**Решение:**

1. Проверьте, что файл `task-data.json` существует
2. Проверьте валидность JSON (используйте jsonlint.com)
3. Проверьте права доступа к файлу

### Агент не использует инструменты

**Проблема:** Агент отвечает общими фразами, не используя MCP tools

**Решение:**

1. Убедитесь, что сервер подключен (статус "connected")
2. Проверьте system prompt — он должен явно инструктировать использовать инструменты
3. Используйте более конкретные запросы
4. Попробуйте модель с лучшей поддержкой function calling (gpt-4o, claude-3.5-sonnet)

### Медленные ответы

**Проблема:** Ассистент долго отвечает

**Решение:**

1. Используйте более быструю модель (gpt-4o-mini вместо gpt-4o)
2. Уменьшите количество загруженных документов в RAG
3. Оптимизируйте RAG threshold (увеличьте до 0.4-0.5)

---

## Дополнительная настройка

### Интеграция с реальной системой задач

Демо-данные из `task-data.json` подходят для тестирования, но для продакшена можно:

1. **Доработать MCP сервер** для работы с API вашей системы (Jira, Linear, GitHub Projects)
2. **Добавить синхронизацию** между Legion и основной системой
3. **Настроить вебхуки** для автоматического обновления данных

### Расширение функционала

Можно добавить дополнительные инструменты:

- `assign_task` — назначение задачи на конкретного члена команды
- `add_comment` — добавление комментариев к задаче
- `link_tasks` — создание зависимостей между задачами
- `get_sprint_report` — отчёт по спринту
- `estimate_task` — помощь в оценке задач

### Персонализация агента

Вы можете создать несколько специализированных агентов:

- **Team Lead Assistant** — фокус на планировании и распределении задач
- **Developer Assistant** — фокус на технических задачах
- **PM Assistant** — фокус на дедлайнах и приоритетах

---

## Полезные команды

Примеры запросов, которые можно попробовать:

```
# Обзор
- Покажи все текущие задачи
- Дай статистику по задачам
- Что у нас в работе?

# Фильтрация
- Покажи задачи с высоким приоритетом
- Какие задачи в статусе todo?
- Покажи все баги

# Поиск
- Найди задачи про RAG
- Есть ли задачи по безопасности?

# Рекомендации
- Что делать первым?
- Какие задачи просрочены?
- Что критично на этой неделе?

# Команда
- Кто больше всего загружен?
- Кому назначить новую задачу?
- Покажи задачи Марии

# Управление
- Создай задачу для добавления темной темы
- Переведи task-5 в статус in-progress
- Назначь task-9 на Ивана
```

---

## Следующие шаги

После успешной настройки:

1. ✅ Протестируйте все сценарии
2. ✅ Загрузите документацию проекта в RAG
3. ✅ Настройте агента под свой стиль работы
4. ✅ Поэкспериментируйте с разными моделями
5. ✅ Добавьте свои задачи в систему

**Готово!** Теперь у вас есть умный ассистент команды. 🎉
